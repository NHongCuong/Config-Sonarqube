# 1. Khai báo thông tin đăng nhập Database
apiVersion: v1
kind: Secret
metadata:
  name: sonar-db-secret
  namespace: sonarqube
  labels:
    app.kubernetes.io/name: sonarqube
    app.kubernetes.io/instance: sonarqube
    app.kubernetes.io/component: database-credentials
    app.kubernetes.io/part-of: sonarqube-system
type: Opaque
stringData:
  SONAR_JDBC_USERNAME: "sonar"
  SONAR_JDBC_PASSWORD: "123456789"

---
# 2. Tạo Service và Endpoint cho Database bên ngoài
apiVersion: v1
kind: Service
metadata:
  name: postgres-external
  namespace: sonarqube
  labels:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/instance: external-db
    app.kubernetes.io/component: database
spec:
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432

---
apiVersion: v1
kind: Endpoints
metadata:
  name: postgres-external
  namespace: sonarqube
subsets:
  - addresses:
      - ip: 192.168.88.133
    ports:
      - port: 5432

---
# 3. Deployment SonarQube với NodeSelector
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sonarqube
  namespace: sonarqube
  labels:
    app.kubernetes.io/name: sonarqube
    app.kubernetes.io/instance: sonarqube
    app.kubernetes.io/component: server
    app.kubernetes.io/part-of: sonarqube-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: sonarqube
      app.kubernetes.io/instance: sonarqube
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sonarqube
        app.kubernetes.io/instance: sonarqube
        app.kubernetes.io/component: server
    spec:
      # --- THÊM NODESELECTOR TẠI ĐÂY ---
      nodeSelector:
        kubernetes.io/hostname: hongcuong
      # --------------------------------
      initContainers:
        - name: init-sysctl
          image: busybox
          command: ["sysctl", "-w", "vm.max_map_count=262144"]
          securityContext:
            privileged: true
      containers:
        - name: sonarqube
          image: sonarqube:community
          ports:
            - containerPort: 9000
          env:
            - name: SONAR_JDBC_URL
              value: "jdbc:postgresql://192.168.88.133:5432/sonarqube"
            - name: SONAR_JDBC_USERNAME
              valueFrom:
                secretKeyRef:
                  name: sonar-db-secret
                  key: SONAR_JDBC_USERNAME
            - name: SONAR_JDBC_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: sonar-db-secret
                  key: SONAR_JDBC_PASSWORD
           resources:
            limits:
              memory: "4Gi"
              cpu: "2"
            requests:
              memory: "2Gi"
              cpu: "1"
          volumeMounts:
            - name: sonar-data
              mountPath: /opt/sonarqube/data
            - name: sonar-extensions
              mountPath: /opt/sonarqube/extensions
            - name: sonar-logs
              mountPath: /opt/sonarqube/logs
      volumes:
        - name: sonar-data
          persistentVolumeClaim:
            claimName: sonar-pvc
        - name: sonar-extensions
          persistentVolumeClaim:
            claimName: sonar-pvc
        - name: sonar-logs
          persistentVolumeClaim:
            claimName: sonar-pvc
